import 'package:flutter/material.dart';

class SciencesScreen extends StatelessWidget {
  const SciencesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SciencesScreen'),
      ),
    );
  }
}
